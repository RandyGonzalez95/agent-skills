interface:
  display_name: "Workspace Cleanup"
  short_description: "Practical workspace cleanup workflows and review"
  default_prompt: "Use $workspace-cleanup to help with this workspace cleanup task using the context I provide."
policy:
  allow_implicit_invocation: true
