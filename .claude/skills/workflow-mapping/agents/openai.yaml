interface:
  display_name: "Workflow Mapping"
  short_description: "Practical workflow mapping workflows and review"
  default_prompt: "Use $workflow-mapping to help with this workflow mapping task using the context I provide."
policy:
  allow_implicit_invocation: true
