interface:
  display_name: "Brand"
  short_description: "Practical brand workflows and review"
  default_prompt: "Use $brand to help with this brand task using the context I provide."
policy:
  allow_implicit_invocation: true
